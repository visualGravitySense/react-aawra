import React from 'react';

const Home = () => {
  return (
    <div>
      <h1>Featured Courses</h1>
      {/* Promo blocks, banners, platform info, etc. */}
    </div>
  );
};

export default Home;
